using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DandelionController : MonoBehaviour
{
    /*
    [SerializeField] Dandelion[] dandelions;
    private Vector2 durationRange;
    private Vector2 timeRange;
    private Vector3 cyclesRange;

    private void Start()
    {
        foreach (Dandelion dandelionScr in dandelions)
        {
            dandelionScr.Constructor(
                Random.Range(durationRange.x, durationRange.y), 
                Random.Range(durationRange.x, durationRange.y), 
                Random.Range(timeRange.x, timeRange.y), 
                Random.Range(timeRange.x, timeRange.y), 
                (int)Random.Range(cyclesRange.x, cyclesRange.y));
        }
    } */
}
